<!DOCTYPE html>
<html>
	<head>
		<title>Lista de Compras</title>
		<link rel='stylesheet' href='/stylesheets/style.css' />
	</head>
 
	<body>

		<h1>Lista de Compras</h1>

		<p>Não esquecer de comprar os seguintes itens:</p>

		<table style="width:50%">

		 	<thead>
		 		<tr style="background-color: #CCC">
		 			<td style="width:85%">Nome</td>
		 			<td style="width:15%">Qtd.</td>
				</tr>
			</thead>

		 	<tbody>
		 		<% if (!docs || docs.length == 0) { %>
 					<tr>
 						<td colspan="2">Nenhum item cadastrado.</td>
 					</tr>
				<% } else {
						docs.forEach(function(item){ %>
							<tr>
					 			<td><a href="/new/<%= item._id %>"><%= item.nome %></a></td>
					 			<td style="width:20%"><%= item.quantidade %></td>
					 			<td><a href="/delete/<%= item._id %>"
onclick="return confirm('Tem certeza?');"">X</a></td>
					 		</tr>
						<% })
				} %>
		 	</tbody>

			<tfoot>
				<tr>
					<td colspan="3">
						<a href="/new">Cadastrar Novo</a>
					</td>
				</tr>
			</tfoot>

		</table>

	</body>
</html>